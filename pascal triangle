def generate(self, numRows: int) -> List[List[int]]:
        n=numRows
        for i in range(n):
            for j in range(n-i+1):
                print("",end='')
            c=1
            for j in range(1,i):
                print('',c,end='')
                c=c*(i-j)//j
